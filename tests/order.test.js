const order = require('../billing/order');

/** Making a an order
 * id , date , amount , products list
 */

describe('Orders', () => {
  // itx('Must have a product list of at least 1')
  itx('Must have valid fields')
  itx('Must have a date')
});
